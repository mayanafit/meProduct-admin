<%# Inline validation error shown above a form when a submission is rejected. %>
<% if (typeof error === "string" && error) { %>
  <div role="alert" class="mb-4 rounded-md border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">
    <%= error %>
  </div>
<% } %>
